# Allowlist for existing `in_group`/`is_admin` usages outside of the helper definitions.
# Remove entries as group-based checks are refactored away.
app/Controllers/TestAuth.php:22:                    echo "<p>Is Admin: " . ($this->ionAuth->is_admin() ? "Yes" : "No") . "</p>";
