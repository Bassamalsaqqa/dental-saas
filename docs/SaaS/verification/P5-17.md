# Verification: P5-17 — Danger Zone

## Objective
Verify that the `controlplane/danger` routes are correctly registered and the legacy `controlplane/exit` route is removed.

## 1. Route Verification

**Command:**
```bash
php spark routes | findstr /I "controlplane danger exit"
```

**Output:**
```
| POST   | controlplane/danger/exit                  | ??    | \App\Controllers\ControlPlane\Danger::exitGlobalMode          | csrf controlplane                         | controlplane secureheaders csrfjson toolbar                         |
| GET    | controlplane/danger                       | ??    | \App\Controllers\ControlPlane\Danger::index                   | csrf controlplane                         | controlplane secureheaders csrfjson toolbar                         |
```

## 2. Legacy Route Removal Check

**Command:**
```bash
rg -n "controlplane/exit|Exit Global Mode|Terminate" app/Views app/Controllers
```

**Expected Output:**
Only the method definition in `app/Controllers/ControlPlane.php` should appear.

**Actual Output:**
```
app/Controllers/ControlPlane.php
51:    * POST /controlplane/exit
```

## 3. Functionality Check
- Accessing `/controlplane/danger` requires global mode.
- Submitting the form with correct phrase "EXIT GLOBAL MODE" terminates session.
- Submitting with incorrect phrase fails safely.