# Verification: S3-03 Constraints (NOT NULL & Uniques)

## Objective
Enforce `NOT NULL` and FKs.

## 1. Migrations Status
**Command:** `php spark migrate:status`
**Result:** `EnforceClinicIdConstraints` executed.

## 2. Constraints Check
**SQL:** `SHOW CREATE TABLE patients`
**Result:**
- `clinic_id` INT UNSIGNED NOT NULL
- `CONSTRAINT fk_patients_clinics FOREIGN KEY (clinic_id) REFERENCES clinics(id)`

## 3. Unique Constraints
- **Skipped** in this phase to prevent migration failure on existing duplicates. Will be handled in Phase 4 cleanup or manual deduping task.
